{
  "_README": "Copy to .env.json (gitignored) and fill in REAL values; pass via --dart-define-from-file=.env.json on every flutter run/build. NEVER commit a real DSN or secret. SENTRY_DSN empty => crash reporting is inert (NoopCrashReporter).",
  "SUPABASE_URL": "",
  "SUPABASE_ANON_KEY": "",
  "SENTRY_DSN": "",
  "DESIGN_TOOLS": false
}
