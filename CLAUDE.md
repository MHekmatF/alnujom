<!-- SPECKIT START -->
Active Spec Kit feature: `008-locations` (Phase 8 — Locations: governorates, cities, areas)

For technologies, project structure, and shell commands, read the plan:
[specs/008-locations/plan.md](specs/008-locations/plan.md)

Companion artifacts in the same folder:
- `spec.md` — user stories (US1 public-read seed; US2 admin entry tile + three-layer gate; US3 governorate CRUD with bilingual labels + seeded-row protection; US4 city CRUD scoped per governorate + seeded-row protection; US5 area CRUD scoped per city, no protected seed; US6 cascading LocationPicker reusable widget; US7 audit triggers on all three tables), FRs (FR-001..FR-025), 21 success criteria, clarifications (Session 2026-05-16 closed five questions: Q1 no lat/lng columns in Phase 8 — Phase 15 may add them via follow-up migration; Q2 internal hierarchy FKs use `ON DELETE CASCADE` with dialog-enumerated dependent counts; Q3 seeded-row protection via `is_system BOOLEAN` columns on `governorates`+`cities` plus DB immutability triggers mirroring Phase 6's `roles.is_system` precedent — areas have no protected seed; Q4 30–40 city seed coverage covering 14 seats + second-tier known cities of each governorate; Q5 audit triggers attached BEFORE seed inserts so every seeded row produces one `*.created` audit row with `actor_user_id=NULL`)
- `research.md` — 22 locked technical decisions (R-01 migration filename convention carry-forward; R-02 inline policy bundling + parallel policy files; R-03 no new pubspec packages; R-04 public SELECT admits anon + authenticated — deliberate Constitution III carve-out for global reference data; R-05 internal FK CASCADE; R-06 is_system columns; R-07 immutability triggers; R-08 trigger-before-seed audit ordering; R-09 30–40 city target band; R-10 no lat/lng — Phase 15 follow-up; R-11 no optimistic locking on locations writes; R-12 editorial position with alphabetical-by-key tie-break; R-13 log_audit reusable for a fifth time; R-14 current_user_has_permission unchanged for a fourth time; R-15 no new permission keys — `locations.manage` from Phase 6 §9.1 is the only gate; R-16 anonymous SELECT carve-out documented in migration comments; R-17 LocationPicker is_active filtering at data-source level; R-18 display_name fallback chain; R-19 BilingualDisplayNameField widget pattern; R-20 no client-side cache on LocationPicker; R-21 smoke-test surface deferred to implementation; R-22 DEFERRED.md treatment carry-forward)
- `data-model.md` — the 3 new tables (`governorates`, `cities`, `areas`) with full CREATE TABLE bodies + check constraints + indexes; the 9 audit triggers (3 per table) via Phase 4's `log_audit()` unchanged; the 2 immutability triggers (`enforce_governorate_system_immutability`, `enforce_city_system_immutability`) with body source; the 12 RLS policies (4 per table: public SELECT + permission-gated write); the seed inventory (14 governorates + 32 cities + ~10 starter areas); the BLoC + entity + use-case shapes for the new `lib/features/locations/` feature folder; the ARB-key inventory (~25 page/dialog/error keys); the read-side query patterns; the LocationsFailure → ARB-key map
- `contracts/` — 9 interface contracts: `phase8-tables` (3 tables with column/constraint/RLS shapes), `phase8-audit-triggers` (9 triggers reusing log_audit + trigger-before-seed ordering), `phase8-immutability-triggers` (2 BEFORE-UPDATE-OR-DELETE triggers refusing DELETE / key-rename on is_system=true), `phase8-rls-policies` (12 policies + anon-SELECT carve-out documentation), `locations-seed` (14+30..40+starter-areas inventory + idempotency contract), `location-picker-widget` (cascading 3-level widget + optional area + locale fallback + is_active filtering), `locations-admin-pages` (4 pages: LocationsListPage, GovernorateDetailPage, CityDetailPage, LocationFormPage + system-row affordance hiding + delete-confirmation dialog with dependent counts), `locations-routing` (admin-home tile + 4 go_router routes + redirect guards), `locations-localization` (ARB keys + bilingual display_name fallback chain + BilingualDisplayNameField widget contract)
- `quickstart.md` — 12-step end-to-end manual verification recipe via Supabase MCP `apply_migration` / `execute_sql` / `get_advisors` + Flutter UI walk on Infinix Note 8 against the remote Supabase project; per-FR / per-SC verification map at the end of the file; Step 12 covers the cross-device propagation test for SC-007 (rename propagates) and SC-021 (mid-session permission revoke via Phase 7 surface)

Predecessors (still relevant — Phase 8 builds on top of them):
- [specs/007-super-admin-roles/plan.md](specs/007-super-admin-roles/plan.md) — Phase 7 super-admin role UI + the role-mutation RPCs + the audit-trigger expansion pattern + the new feature-folder precedent (`lib/features/super_admin/`). Phase 8 reuses the trigger-attach pattern, the per-table policy-file pattern, the BilingualDisplayNameField-style two-input layout, the route-guard helper, and the admin-home tile pattern. Phase 8 does not edit any Phase 7 artifact.
- [specs/006-roles-permissions/plan.md](specs/006-roles-permissions/plan.md) — Phase 6 introduced the `locations.manage` permission key in `public.permissions` and mapped it to `admin` (§9.1) and `super_admin` (full catalog). Phase 8 reuses the key without adding any new permission. Phase 6's `current_user_has_permission(TEXT)` helper is consumed unchanged by every Phase 8 write policy. Phase 6's `enforce_role_system_immutability` trigger is the precedent for Phase 8's two new immutability triggers.
- [specs/005-auth-profile/plan.md](specs/005-auth-profile/plan.md) — Phase 5 auth + admin home. Phase 8 adds one tile to the Phase 6-rehosted `AdminHomePage`; Phase 5 surfaces are not edited.
- [specs/004-supabase-foundation/plan.md](specs/004-supabase-foundation/plan.md) — Phase 4 `log_audit()` + `set_updated_at()` reusable trigger functions. Phase 8 attaches three new trigger groups via the unchanged `log_audit` function (R-05 reusability invariant preserved a **fifth** time across Phases 4/5/6/7/8) and attaches `set_updated_at` to all three new tables unchanged.
- [specs/003-localization/plan.md](specs/003-localization/plan.md) — Phase 3 ARB-driven localization. Phase 8 adds ~25 new ARB keys for the locations admin pages and the LocationPicker widget; the bilingual `display_name` JSONB pattern from Phase 6 is reused for data labels.
- [specs/002-design-system/plan.md](specs/002-design-system/plan.md) — Phase 2 design tokens consumed by every new widget in `lib/features/locations/presentation/`.

Cross-cutting:
- [docs/decisions/0001-secrets-and-pii-storage.md](docs/decisions/0001-secrets-and-pii-storage.md) — Supabase Vault for secrets and admin-only PII. Phase 8 does NOT introduce any PII or secret storage; the locations catalog is global reference data with no privacy concern.
- Forward-stated Phase 10 contract: when Phase 10 introduces `listings.city_id` and `listings.area_id`, those FKs MUST use `ON DELETE RESTRICT` (with `ON DELETE SET NULL` acceptable for `area_id` since area is optional). Phase 8's internal-hierarchy `ON DELETE CASCADE` therefore never silently nukes listing rows. This is documented in Phase 8 spec FR-002 and Edge Cases.
- Forward-stated Phase 13 contract: Phase 13 (Public listing details) will consume the public-read SELECT path on all three locations tables. Phase 8's anon-allowed SELECT policy makes this possible without further schema changes.
- Forward-stated Phase 14 / Phase 15 contract: Phase 14 (Search & filters) consumes the LocationPicker as a filter widget; Phase 15 (Map view) MAY add `latitude`/`longitude` columns to `cities`/`areas` via a non-breaking follow-up migration if real demand emerges (per spec Clarifications Q1).
- Phase 22 follow-up (project memory `project_phase22_perm_cache_revisit.md`): Phase 22's push + Realtime will revisit Phase 6's three-point cache refresh; Phase 8's mutation surface (admin grants / revokes the `locations.manage` permission via Phase 7's RoleEditorPage; admin creates / renames / deactivates locations rows) adds another data point worth examining at that time, though Phase 8 itself adds no new propagation work — the LocationPicker reads fresh on each mount (R-20).
<!-- SPECKIT END -->
