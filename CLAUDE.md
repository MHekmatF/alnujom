<!-- SPECKIT START -->
Active Spec Kit feature: `012-listing-approval` (Phase 12 — Listing Approval Workflow)

For technologies, project structure, and shell commands, read the plan:
[specs/012-listing-approval/plan.md](specs/012-listing-approval/plan.md)

Companion artifacts in the same folder:
- `spec.md` — user stories (US1 admin reviews pending queue + approves; US2 admin rejects with reason + publisher resubmits; US3 public-read RLS end-to-end verification with status-revert visibility flip; US4 audit + status-history completeness; US5 admin queue pagination/sort/refresh; US6 publisher rejection banner + Resubmit + moderation history), FRs (FR-001..FR-024 — FR-024 is the Q7=A trigger + log_audit amendment), 32 success criteria (SC-001..SC-032), 8 clarifications resolved across `/speckit-specify` (Q1=B Edge Function, Q2=A NULL expires_at, Q3=A Minimal-5+Other presets) + `/speckit-clarify` (Q4=A JSON-encoded TEXT reason, Q5=A UX-required-when-Other detail, Q6=A ≤2s p95 latency, Q7=A session-variable amendment, Q8=A shared widget paths under lib/shared/presentation/widgets/listing_display/)
- `research.md` — 20 locked plan-time decisions (R-41 Edge Function file layout via Supabase MCP deploy; R-42 JWT-bound permission check + service-role privileged UPDATE pattern; R-43 session-variable handoff design with 2 setter SECURITY DEFINER functions + COALESCE in 3 amended functions; R-44 Q4=A JSON-encoded TEXT storage rep; R-45 Phase 6 permission catalog audit confirms zero new permission keys needed; R-46 local-dev Edge Function invocation via supabase functions serve; R-47 reject_preset_<key> ARB naming convention; R-48 (submitted_at, id) cursor pagination shape; R-49 Phase 10 trigger function name audit — listing_status_transition_trigger_fn + listings_audit_trigger_fn; R-50 cached_network_image consumption pattern for ListingGallery via Phase 11 getPublicUrl() URLs; R-51 sticky bottom-bar approve/reject CTAs sized for Infinix Note 8 / Pixel 8 Pro portrait; R-52 Edge Function error JSON → Dart Failure typed mapping; R-53 BLoC ownership boundary Phase 12 preview vs Phase 13 listing-details; R-54 concurrent admin race verification approach; R-55 two-device manual verification matrix per user story; R-56 listing_status_history.reason TEXT column verification; R-57 SECURITY DEFINER safety analysis for session-var setters; R-58 moderation history page reads listing_status_history NOT audit_logs; R-59 no CORS configuration in v1; R-60 single-migration apply order. Plus carried-forward invariants from prior phases R-01..R-40; R-05 narrowly relaxed; R-36 explicitly overridden in Phase 12.
- `data-model.md` — FULL FR-024 migration body inline including: 2 new session-variable setter SECURITY DEFINER functions; the 3 amended function bodies (listing_status_transition_trigger_fn / listings_audit_trigger_fn / log_audit) showing exactly what changes from Phase 10/4 originals; Edge Function call sequences in TypeScript for both approve_listing and reject_listing; Dart entity / DTO / use case / BLoC event-state shapes; 5 new Failure subtypes in lib/core/errors/failure.dart; the 32-key ARB inventory with ar+en bilingual labels; per-FR / per-SC verification map covering all 32 SCs
- `contracts/` — 10 interface contracts: `phase12-approve-listing-edge-function` (POST body + response shapes + error codes + call sequence + 2s p95 budget); `phase12-reject-listing-edge-function` (POST body + 6-preset validator + Q4=A JSON build + UPDATE predicate); `phase12-trigger-and-log-audit-amendment` (FR-024 contract: 3 functions amended + 2 setter functions + R-05 narrow relaxation verification); `phase12-rejection-reason-storage-format` (Q4=A JSON-encoded-TEXT shape + read pattern + forward JSONB conversion path); `phase12-admin-queue-page` (queue route + card composition + cursor pagination contract); `phase12-listing-preview-page` (preview composition consuming Q8=A widgets + sticky bottom bar CTAs + dialog launches + Failure → toast mapping); `phase12-shared-display-widgets` (Q8=A 5 widgets API surface + Constitution IX/V/VI compliance + Phase 13 forward-state contract); `phase12-reject-reason-dialog` (FR-013 dialog including Q5=A Other-UX-required gate + Q3=A 6 presets); `phase12-publisher-rejection-banner` (FR-015 banner + Q4=A JSON parse + admin-identity-suppression); `phase12-moderation-history-page` (FR-017 read-only page + admin-identity-suppression rule)
- `quickstart.md` — 19-step end-to-end manual verification recipe via Supabase MCP `apply_migration` / `execute_sql` / `deploy_edge_function` + two-device Flutter UI walk (Pixel 8 Pro emulator as admin device + Infinix Note 8 as publisher device); covers FR-024 migration apply + 2 Edge Function deploys + Phase 6 permission-seed audit + admin queue first load + listing preview full-fidelity + reject path with Q4=A JSON verification + audit + attribution under Q7=A session-var amendment + public read RLS draft path + approve path with anonymous storage download + reject dialog UX + publisher rejection banner + Resubmit deep-link + moderation history page + route guard for unpermitted + non-admin Edge Function call + concurrent admin race producing already_acted_on + media inaccessible on status revert + reject-resubmit-reject chain audit completeness + approve-revert-approve chain audit completeness + final SC matrix coverage

Predecessors (still relevant — Phase 12 builds on top of them):
- [specs/011-media-watermark/plan.md](specs/011-media-watermark/plan.md) — Phase 11 listing_media + 2 storage buckets + 6×2 storage RLS policies + cap trigger. Phase 12 verifies Phase 11's storage RLS honors parent-status flips per Phase 11 SC-025 — no edits to Phase 11 policies. Phase 12 consumes Phase 11's `listing_media` rows in the admin preview's `ListingGallery` widget AND in the queue card's main-image thumbnail. Phase 12's reverses Phase 11's R-36 ("zero new Edge Functions") — Phase 12 introduces the project's first listings-domain Edge Functions per Q1=B.
- [specs/010-listing-creation/plan.md](specs/010-listing-creation/plan.md) — Phase 10 listing-status engine + the 5 listing tables + submit_listing RPC + listing_status_transition_trigger_fn + listings_audit_trigger_fn. Phase 12 amends BOTH trigger functions via a new Phase 12 migration (Phase 10 file remains unedited per R-35) — adding COALESCE on the actor source AND (for the status-transition trigger only) COALESCE on the reason source via session variables. Phase 12 verifies Phase 10's public-read RLS on public.listings honors `status='approved'` end-to-end — no policy edit.
- [specs/009-currencies/plan.md](specs/009-currencies/plan.md) — Phase 9 MoneyFormatter consumed unchanged by the Q8=A `ListingPriceBlock` shared widget. No new currency surface.
- [specs/008-locations/plan.md](specs/008-locations/plan.md) — Phase 8 governorate/city/area names consumed unchanged by the Q8=A `ListingLocationBlock` shared widget AND by the queue card composition.
- [specs/007-super-admin-roles/plan.md](specs/007-super-admin-roles/plan.md) — Phase 7 R-06 RPC-over-Edge-Function decision was bounded to role mutations. Phase 12 does NOT follow R-06 for approve/reject because IMPLEMENTATION_PLAN §6.7 explicitly names them as Edge Functions per Q1=B; future phases MAY revert to RPCs for non-notification-bearing mutators.
- [specs/006-roles-permissions/plan.md](specs/006-roles-permissions/plan.md) — Phase 6 permission keys (`listings.approve`, `listings.reject`) consumed by Phase 12's Edge Function permission re-check via Phase 6's `current_user_has_permission(perm_key)` RPC. R-45 audit confirms both keys are already seeded with role mappings for moderator/admin/super_admin — zero new permission keys (FR-009-equivalent).
- [specs/005-auth-profile/plan.md](specs/005-auth-profile/plan.md) — Phase 5 introduced the project's first Edge Functions (lookup_email_by_phone, request_password_reset). Phase 12 follows the same `supabase/functions/<name>/index.ts` layout pattern + the same `@supabase/supabase-js@2` import style. Phase 12 is the FIRST listings-domain Edge Functions, not the project's first ever.
- [specs/004-supabase-foundation/plan.md](specs/004-supabase-foundation/plan.md) — Phase 4 `log_audit()` reusable trigger function. Phase 12 NARROWLY amends `log_audit()` via a new Phase 12 migration per Q7=A — single-line COALESCE on the actor source ONLY. R-05 byte-identical-reuse invariant relaxed to "byte-identical except for the actor-source COALESCE"; every Phase 5–11 caller continues to attribute correctly because the COALESCE falls back to `auth.uid()` when the session variable is unset.
- [specs/003-localization/plan.md](specs/003-localization/plan.md) — Phase 3 ARB-driven localization. Phase 12 adds ~32 new ARB keys for queue chrome, preview CTAs, the approve confirmation dialog, the reject-reason dialog, the 6 Q3=A preset labels, the publisher rejection banner, the moderation history page (including 9 status-transition labels), structured-error toasts, status-flip success toasts.
- [specs/002-design-system/plan.md](specs/002-design-system/plan.md) — Phase 2 design tokens consumed by every new widget under `lib/features/admin/listing_review/presentation/` + `lib/features/publisher_dashboard/presentation/` + `lib/shared/presentation/widgets/listing_display/`.
- [specs/001-project-foundation/plan.md](specs/001-project-foundation/plan.md) — Phase 1 `cached_network_image` consumed by the Q8=A `ListingGallery` widget against Phase 11's public-bucket URLs from `supabase.storage.from('listing-images').getPublicUrl(path)`. Phase 12 is the first widget to actually consume the public-URL pattern Phase 11 R-29 forward-stated.

Cross-cutting:
- [docs/decisions/0001-secrets-and-pii-storage.md](docs/decisions/0001-secrets-and-pii-storage.md) — Phase 12 introduces NO new Vault columns. The service-role key used inside the Edge Functions was already provisioned during Phase 4's Vault scaffolding.
- Forward-stated Phase 13 contract: Phase 13's public listing-details page MUST import the Q8=A shared widgets at `lib/shared/presentation/widgets/listing_display/` verbatim — no second implementation, no drift. Phase 13 ships its own `ListingDetailsBloc` consuming the same widgets; per R-53 the two BLoCs are independent.
- Forward-stated Phase 14 contract: Phase 14's search-result thumbnails consume the same Phase 11 `getPublicUrl()` URL pattern. Phase 14's filters operate on `status='approved'` — the same predicate Phase 12 verifies in US3.
- Forward-stated Phase 17 contract: Phase 17 (or similar) MAY introduce a `notifications` table + the Edge Function mutators FR-001/FR-002 are the natural future-spec attachment site for notification emission. Phase 12 itself does NOT ship any notification subsystem per Q5-folded-default + FR-019.
- Forward-stated Phase 22 contract: Phase 22 push + Realtime MAY add a Realtime subscription on `public.listings.status` so the publisher dashboard updates without manual refresh. Phase 22 will also revisit the Phase 6 three-point cache refresh per `project_phase22_perm_cache_revisit.md`.
- Forward-stated Phase 23 contract: Phase 23 admin settings MAY introduce audit-log retention controls AND MAY introduce an `app_settings.default_listing_expiry` row that overrides Phase 12's Q2=A `expires_at = NULL` default.
- Forward-stated v2-revisit: a future spec MAY convert `listing_status_history.reason TEXT` to JSONB if cross-row analytic queries on the preset key become hot. The Q4=A JSON-encoded TEXT is forward-compatible — the conversion is a single `ALTER TABLE ALTER COLUMN reason TYPE JSONB USING reason::jsonb` with no row-by-row backfill needed.
- R-05 narrow relaxation: from Phase 12 forward, "Phase 4 `log_audit()` byte-identical reuse" means "byte-identical EXCEPT for the actor-source COALESCE amendment on `actor_user_id`". Phase 13+ should continue to use `log_audit()` unchanged; the COALESCE makes the function transparently support both Edge Function callers (session-variable path) AND direct user-JWT callers (`auth.uid()` fallback).
<!-- SPECKIT END -->
