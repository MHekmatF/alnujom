<!-- SPECKIT START -->
Active Spec Kit feature: `013-home-and-details` (Phase 13 — Public Home & Listing Details)

For technologies, project structure, and shell commands, read the plan:
[specs/013-home-and-details/plan.md](specs/013-home-and-details/plan.md)

Companion artifacts in the same folder:
- `spec.md` — user stories (US1 anonymous browses paginated home feed; US2 anonymous opens listing details with gallery swipe + video tap; US3 authenticated browses same surfaces; US4 public-read RLS end-to-end UI verification; US5 cursor pagination correctness under concurrent writes; US6 visual + design-token + localization compliance), FRs (FR-001..FR-035), 35 success criteria (SC-001..SC-035), 6 clarifications resolved across `/speckit-specify` (Q1=A search/property-type stub snackbars, Q2=A all six listing-details CTAs are Coming-soon stubs, Q3=A anonymous-tap forward-state convention) + `/speckit-clarify` (Q4=D `ListingDetailsPage` conditional back-button, Q5=A ≤2s p95 steady-state latency, Q6=A no-auto-refresh on background→foreground resume)
- `research.md` — 12 locked plan-time decisions R-61..R-72 (R-61 index migration filename + apply order; R-62 cursor wire format via two separate `lt` filters; R-63 home-feed SELECT projection shape with embedded selects; R-64 listing-details SELECT projection shape; R-65 ship Phase-13-specific `HomeListingCard` widget vs reuse Phase 2's `ListingCard`; R-66 shimmer/skeleton plugin deferred — use `cached_network_image` placeholder; R-67 time-since-publish via `intl` `RelativeDateTime`; R-68 `ListingNotFoundFailure` Dart type; R-69 `AppRoutes.shellHome` rename with interim alias; R-70 `ListingDetailsBloc` independent of Phase 12's `ListingPreviewBloc` per R-53; R-71 `DeepLinkAwareBackButton` as inline helper now, extracted on second consumer; R-72 two-device manual verification matrix per user story — Infinix Note 8 primary for anonymous-browse, Pixel 8 Pro emulator secondary). Plus carried-forward invariants R-01..R-60.
- `data-model.md` — FULL index migration body (4 `CREATE INDEX IF NOT EXISTS` statements) + home-feed SELECT projection shape + Dart DTO + entity for `HomeListingCard` + `Cursor` value object + listing-details SELECT projection + `ListingDetailsAggregate` entity + `ListingNotFoundFailure` extension to `lib/core/errors/failure.dart` + FULL 25-key bilingual ARB inventory (ar + en) with Q1=A snackbar keys + Q2=A snackbar keys + Q3=A reserved keys + chrome + CTA labels + error states + empty states + per-FR / per-SC verification map covering all 35 SCs.
- `contracts/` — 7 interface contracts: `phase13-index-migration` (4 indexes + idempotency + EXPLAIN expected output); `phase13-home-feed-query` (SELECT projection + cursor predicate + RLS-only filter discipline); `phase13-listing-details-query` (SELECT projection with embedded selects + null-handling for "Listing not found"); `phase13-home-page-composition` (AppBar + hero search + property-type chips + paginated feed + empty-state + Q1=A snackbar wiring); `phase13-listing-details-page-composition` (Q8=A widget composition + Contact block + per-listing-action block + Q4=D + Q2=A wiring); `phase13-deep-link-back-button` (Q4=D conditional pattern + PopScope wrapper + forward-state convention); `phase13-cta-stub-treatment` (Q1=A + Q2=A unified snackbar treatment + Q3=A reserved-keys forward convention).
- `quickstart.md` — 19-step end-to-end manual verification recipe: apply index migration via Supabase MCP `apply_migration` + EXPLAIN check + grep gates (RLS-only filter / Supabase isolation / design tokens / Q8=A widget reuse / dependency surface) + two-device Flutter UI walk (Infinix Note 8 primary anonymous-browse + Pixel 8 Pro emulator secondary). Covers HomePage cold launch + Q1=A snackbars + infinite-scroll pagination + pull-to-refresh + two-device concurrent-approve race + card tap navigation + gallery swipe + video tap external launch + Q2=A snackbars + Q4=D back-button deep-link via `adb shell am start` + Q6=A background→foreground resume + RLS verification end-to-end at the UI + 4-combination visual check + status-revert propagation + final SC matrix coverage.

Predecessors (still relevant — Phase 13 builds on top of them):
- [specs/012-listing-approval/plan.md](specs/012-listing-approval/plan.md) — Phase 12 Q8=A five shared display widgets at `lib/shared/presentation/widgets/listing_display/` (ListingGallery, ListingPriceBlock, ListingLocationBlock, ListingAmenitiesBlock, ListingDescriptionBlock) are CONSUMED VERBATIM by Phase 13's `ListingDetailsPage` — NO edits per SC-016. Phase 12's `approve_listing` Edge Function is the only writer of `status='approved'` rows; Phase 13's home feed has no content until Phase 12 has merged AND at least one approval has succeeded. Phase 12 R-53 BLoC ownership boundary: Phase 13's `ListingDetailsBloc` is INDEPENDENT of Phase 12's `ListingPreviewBloc` per R-70.
- [specs/011-media-watermark/plan.md](specs/011-media-watermark/plan.md) — Phase 11 `public.listing_media` table + `listing-images` Storage bucket + anonymous-Storage RLS gating non-approved listings. Phase 13's home-feed embedded select + details embedded select both read from `listing_media`; Phase 11's `getPublicUrl()` URL pattern (R-29) is actively consumed for the first time at the public surface by the Phase 12 Q8=A `ListingGallery` widget. Phase 11's deferred external-player launch (DEFERRED.md line 45) is RESOLVED in Phase 13 via FR-027 + R-71 (`url_launcher` for video tap). Phase 11 Q2=D external_link UI deferral preserved — Phase 13 ships zero external-link authoring surface.
- [specs/010-listing-creation/plan.md](specs/010-listing-creation/plan.md) — Phase 10 `public.listings` table + `public.listing_prices` + `public.listing_details` + Listing/ListingDetails/ListingPrice Dart entities + the public-read RLS policy (`status='approved' AND publish-window-open`) which is the SOLE filter for Phase 13's queries per FR-018 + SC-008. Phase 13 introduces zero schema edits to these tables. Phase 10's `MyListingsPage` remains unchanged — Phase 13's HomePage shows only public-approved listings.
- [specs/009-currencies/plan.md](specs/009-currencies/plan.md) — Phase 9 `MoneyFormatter` consumed by Phase 13's `_HomeListingCard` AND by the Phase 12 Q8=A `ListingPriceBlock`. Cross-currency conversion via Phase 9's `exchange_rates` table.
- [specs/008-locations/plan.md](specs/008-locations/plan.md) — Phase 8 governorate/city/area entities + their `name_ar` / `name_en` localized fields consumed by Phase 13's `_HomeListingCard` AND by the Phase 12 Q8=A `ListingLocationBlock`.
- [specs/005-auth-profile/plan.md](specs/005-auth-profile/plan.md) — Phase 5's `AuthBloc` consumed by Phase 13's HomePage (sign-in icon + empty-state CTA branching) AND by `ListingDetailsBloc` (auth-state observation for the Q3=A forward-state convention). The Phase 5 login route is the destination for the "Sign in" snackbar action per Q3=A.
- [specs/003-localization/plan.md](specs/003-localization/plan.md) — Phase 3 ARB-driven localization. Phase 13 adds ~25 new keys: HomePage chrome (6) + Q1=A snackbars (2) + Q2=A snackbars (6) + Q3=A reserved (2) + ListingDetailsPage chrome (3) + CTA labels (6) + error states (4) + empty-state CTAs (2). The hand-maintained `app_strings.dart` `_DebugAppLocalizations` subclass is extended with concrete getters per the Phase 11 DEFERRED.md forward-stated note. Time-since-publish via `intl` `RelativeDateTime` (Phase 3 dependency consumed for the first time in Phase 13 per R-67).
- [specs/002-design-system/plan.md](specs/002-design-system/plan.md) — Phase 2 design tokens consumed by every new widget under `lib/features/home/presentation/` + `lib/features/listing_details/presentation/`. The R-66 shimmer/skeleton decision uses `cached_network_image` placeholder (no new shimmer plugin).
- [specs/001-project-foundation/plan.md](specs/001-project-foundation/plan.md) — Phase 1's `lib/shell/shell_home_page.dart` is DELETED in Phase 13 per FR-009 + SC-010 (the explicit forward-stated contract from `specs/001-project-foundation/contracts/app-router.md` line 10). Phase 1's `cached_network_image` dependency is consumed at the public surface for the first time. Phase 1's `go_router` route surface is extended with `/listings/:id` + the `/` builder is rewired from `ShellHomePage` to `HomePage`. Phase 1's `lib/core/network/supabase_client.dart` wrapper consumed unchanged. Phase 1's `Result<T>` / `Failure` types extended with `ListingNotFoundFailure` per R-68.

Cross-cutting:
- [docs/decisions/0001-secrets-and-pii-storage.md](docs/decisions/0001-secrets-and-pii-storage.md) — Phase 13 reads publisher's `full_name` + `username` only; private Vault columns (`legal_name`, `national_id`, `private_contact_methods`) are NOT projected. Phase 5 RLS would block them anyway, but the explicit projection prevents accidental exposure.
- One new pubspec dependency: `url_launcher: ^6.x` introduced solely for the FR-027 video-tap external-player launch (resolving Phase 11's deferred external-player item). NO `share_plus` per Q2=A FR-033 — share is a Coming-soon stub in Phase 13.
- Q4=D `Navigator.canPop() ? pop : context.go(AppRoutes.home)` is the project-wide FORWARD-STATE CONVENTION for every later-phase deep-link-targetable page (future Phase 14 search-result detail, future Phase 22 push-notification target). Per R-71 Phase 13 ships the pattern as an inline helper; extracted to a reusable widget at `lib/core/widgets/deep_link_aware_back_button.dart` upon the second consumer.
- Q3=A `auth_required_please_sign_in` + `auth_required_sign_in_action` ARB keys are the project-wide FORWARD-STATE CONVENTION for every later-phase auth-required CTA (Phase 16 inquiry form; future favorites/reports phases). Phase 13 reserves the keys in its ARB delta but does not surface them.
- Forward-stated Phase 14 contract: Phase 14 will replace Phase 13's Q1=A-stubbed hero-search-bar + property-type-shortcuts with the fully-wired search + filter + sort surface, owning a new `/search` route + `tsvector` infrastructure. The Phase 13 indexes `idx_listings_governorate_status` + `idx_listings_property_type_status` (shipped now) support Phase 14's facet-filter patterns.
- Forward-stated Phase 15 contract: Phase 15 ships the map view at `/map`. Phase 15 may add a "View on map" CTA to `ListingDetailsPage` (consumer-of-listing-details concern, not a Phase 13 edit).
- Forward-stated Phase 16 contract: Phase 16 wires the Q2=A-stubbed Call / WhatsApp / Send-inquiry CTAs via `url_launcher` (`tel:` + `https://wa.me/`) + the new inquiry form + `record_lead_event` Edge Function. Phase 16 MUST adopt the Q3=A forward-state convention for the auth-required inquiry-form CTA.
- Forward-stated favorites + reports + share-wiring phase contracts: Each later phase wires its respective Q2=A-stubbed CTA. The share-wiring phase introduces `share_plus`. All MUST cite Q3=A by reference.
- Forward-stated Phase 22 contract: Phase 22 adds Realtime subscription on `public.listings.status='approved'` to make the home feed live without manual pull-to-refresh — overrides Q6=A's no-auto-refresh baseline. Phase 22 also wires push-notification deep-links that land on `ListingDetailsPage` via the `/listings/:id` route — exercises Q4=D conditional back-button extensively.
- Forward-stated Phase 24 contract: Phase 24 (observability) may instrument the SC-001 / SC-004 / SC-034 / SC-035 cold-launch + interaction-latency metrics for production telemetry.
- R-05 narrow relaxation (from Phase 12): Phase 13 ships zero new `log_audit()` call sites; the function body is consumed unchanged per the relaxed invariant.
<!-- SPECKIT END -->
