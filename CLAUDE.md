<!-- SPECKIT START -->
Active Spec Kit feature: `014-search-filters` (Phase 14 — Search & Filters)

For technologies, project structure, and shell commands, read the plan:
[specs/014-search-filters/plan.md](specs/014-search-filters/plan.md)

Companion artifacts in the same folder:
- `spec.md` — 5 user stories (US1 keyword search; US2 facet filters; US3 inline sort; US4 filter state persistence through navigation; US5 combined search + filter + sort), FRs (FR-001..FR-019), 11 success criteria (SC-001..SC-011), 5 clarifications resolved in `/speckit-clarify` (Q1=A Arabic exact-token + user-education hint + smart-search deferred; Q2 custom dual-mode rooms/bathrooms "Exactly N" / "At least N"; Q3=A modal bottom sheet; Q4=A inline sort control; Q5=A pre-filtered from property-type chip)
- `research.md` — 12 locked plan-time decisions R-73..R-84 (R-73 tsvector covers title+address_text only; description via ILIKE supplement; R-74 SQL RPC approach + SECURITY DEFINER; R-75 price-range currency conversion client-side via latest_rates_for_base RPC; R-76 two cursor shapes — NewestCursor / PriceCursor discriminated union; R-77 BLoC lifetime at route scope for filter-state persistence; R-78 DraggableScrollableSheet for filter bottom sheet; R-79 SegmentedButton<CountFilterMode> + stepper for rooms/bathrooms; R-80 GoRouterState.extra for PropertyType pre-filter; R-81 v_listings_public view pre-joins price + main image + location names; R-82 app_strings.dart extended with 34 new Phase 14 getters; R-83 location data lazy-loaded on sheet open via Phase 8 LocationRepository; R-84 DropdownButton<SortOrder> for inline sort control)
- `data-model.md` — FULL SQL migration bodies (search_vector column + GIN index; v_listings_public view; search_listings RPC with composite return type) + Dart domain entity definitions (SearchResultItem, FilterState with copyWith, SortOrder enum, CountFilterMode enum, SearchCursor sealed class) + FULL 34-key bilingual ARB inventory (ar + en) with section-by-section breakdown + per-FR / per-SC verification map covering all 19 FRs and 11 SCs
- `contracts/` — 6 interface contracts: `phase14-search-vector-migration` (tsvector column + GIN index SQL spec + EXPLAIN expected output + idempotency requirements); `phase14-search-listings-rpc` (full 22-parameter list + return type + behavioral contracts for keyword/facet/price/rooms/cursor/sort + smoke test queries); `phase14-filter-sheet` (SearchFilterSheet DraggableScrollableSheet composition + all 9 filter dimensions + Apply/Reset behavioral contract + PriceRangeInput inline validation); `phase14-search-page-composition` (SearchPage widget tree + _SearchBar auto-focus contract + _SortAndFiltersRow + InlineSortControl + SearchResultCard + empty/error states + entry-point behavior table); `phase14-filter-state-persistence` (BLoC lifetime mechanism + invariants + lifecycle diagram + DeepLinkAwareBackButton second-consumer note); `phase14-home-rewiring` (hero_search_bar + property_type_shortcut_row before/after tap-handler delta + ARB key preservation rules)
- `quickstart.md` — 19-step end-to-end manual verification recipe: apply 3 migrations + EXPLAIN check + RPC smoke tests + grep gates (no app-layer approved filter / Supabase isolation / design tokens / no hardcoded strings) + two-device Flutter UI walk (Infinix Note 8 primary, Pixel 8 Pro emulator secondary). Covers hero search bar entry (SC-011) + chip entry pre-filter (SC-011) + Arabic exact-token search + Arabic hint (SC-001) + Latin search (SC-002) + facet filters Apply/Reset (SC-003) + dual-mode rooms (SC-003) + inline sort reorder (SC-004) + back-navigation state restoration (SC-005) + empty state (SC-006) + price-range validation (SC-009) + pagination (SC-010) + anonymous access (SC-008) + RTL/LTR (SC-007) + final SC matrix

Predecessors (still relevant — Phase 14 builds on top of them):
- [specs/013-home-and-details/plan.md](specs/013-home-and-details/plan.md) — Phase 13 ships `hero_search_bar.dart` and `property_type_shortcut_row.dart` as Q1=A Coming-soon stubs; Phase 14 Sub-Phase G replaces those stub handlers with real navigation. Phase 13's `idx_listings_governorate_status` + `idx_listings_property_type_status` indexes (R-61) are already applied and support Phase 14's facet-filter EXPLAIN paths. Phase 13's `AppRoutes.listingDetails` route is the destination for `SearchResultCard` tap events. Phase 13 Q4=D `Navigator.canPop()` inline back-button pattern — Phase 14's `SearchPage` is the second consumer; per R-71 the extracted `DeepLinkAwareBackButton` widget at `lib/core/widgets/deep_link_aware_back_button.dart` may be created now.
- [specs/012-listing-approval/plan.md](specs/012-listing-approval/plan.md) — Phase 12's `approve_listing` Edge Function is the only writer of `status='approved'` rows; Phase 14's search results have no content until Phase 12 has merged AND at least one approval has succeeded.
- [specs/010-listing-creation/plan.md](specs/010-listing-creation/plan.md) — Phase 10 `public.listings` + `public.listing_prices` + `public.listing_details` table schemas; `PropertyType` + `ListingPurpose` Dart enums consumed by `FilterState` and `SearchResultItem`. The `public.listing_details` table's `rooms`, `bathrooms`, `area_size`, `description` columns are the target of Phase 14's facet filters.
- [specs/009-currencies/plan.md](specs/009-currencies/plan.md) — Phase 9 `latest_rates_for_base` RPC (file: `supabase/migrations/20260518120006_create_latest_rates_for_base_rpc.sql`) consumed by Phase 14's `SupabaseSearchDatasource` for price-range currency pre-conversion (R-75). Phase 9 `CurrencyRepository` injected into `SearchFilterSheet` for the currency selector.
- [specs/008-locations/plan.md](specs/008-locations/plan.md) — Phase 8 `LocationRepository` + `Governorate` / `City` / `Area` entities consumed by `SearchFilterSheet`'s cascading location picker (R-83). Governorate/city `name_ar` / `name_en` fields flow into `v_listings_public` view and `SearchResultItem`.
- [specs/005-auth-profile/plan.md](specs/005-auth-profile/plan.md) — Phase 14 search is anonymous-accessible (FR-015); no `AuthBloc` dependency needed for search itself. The Phase 5 login route remains the destination for any future auth-required CTAs per the Q3=A forward-state convention.
- [specs/003-localization/plan.md](specs/003-localization/plan.md) — Phase 3 ARB-driven localization. Phase 14 adds 34 new keys across search page chrome, sort options, filter sheet, empty/error states, and Arabic hint (FR-019).
- [specs/002-design-system/plan.md](specs/002-design-system/plan.md) — Phase 2 design tokens consumed by `SearchPage`, `SearchFilterSheet`, `SearchResultCard`, `InlineSortControl`, and `PriceRangeInput`. No inline hex / font-size / padding.
- [specs/001-project-foundation/plan.md](specs/001-project-foundation/plan.md) — Phase 1's `go_router` route surface extended with `/search` route; `lib/core/network/supabase_client.dart` wrapper consumed unchanged for RPC calls.

Cross-cutting:
- [docs/decisions/0001-secrets-and-pii-storage.md](docs/decisions/0001-secrets-and-pii-storage.md) — `search_result_row` composite type and `SearchResultItem` entity do NOT include publisher private fields (legal name, national ID, private contact methods). The `v_listings_public` view projects only public-safe columns.
- Zero new pubspec packages for Phase 14. All needed packages (supabase_flutter, flutter_bloc, go_router, get_it + injectable, intl, cached_network_image, equatable) already in pubspec.yaml.
- Phase 14 is the second consumer of the Q4=D `Navigator.canPop()` back-button pattern (Phase 13 R-71 forward-state). Extraction of `DeepLinkAwareBackButton` to `lib/core/widgets/deep_link_aware_back_button.dart` is **deferred** to the phase that adds the third consumer — see `specs/014-search-filters/DEFERRED.md §D-001`.
- Q3=A `auth_required_please_sign_in` + `auth_required_sign_in_action` ARB keys (reserved in Phase 13) remain unused in Phase 14 — search is fully anonymous. Phase 16 wires them.
- Forward-stated Phase 15 contract: Phase 15 ships the map view at `/map`. Phase 15 may add a "View on map" CTA to `SearchResultCard` or `ListingDetailsPage`.
- Forward-stated Phase 16 contract: Phase 16 wires the Call / WhatsApp / inquiry CTAs from `ListingDetailsPage`. Phase 16 MUST cite the Q3=A forward-state convention.
- Forward-stated smart Arabic search: A future phase SHOULD add morphological Arabic search (PostgreSQL Arabic stemming or client-side query expansion). FR-003 and SC-001 in `spec.md` are the primary revision targets. Phase 14's `tsvector` infrastructure using `'simple'` config is the starting point.
- Forward-stated Phase 22 contract: Phase 22 adds Realtime subscription + push-notification deep-links. Phase 14's `/search` route and `SearchPage` deep-link behavior (back-button convention) are relevant.
<!-- SPECKIT END -->
