<!-- SPECKIT START -->
Active Spec Kit feature: `011-media-watermark` (Phase 11 — Listing Media Upload, Client-Side Watermark & Storage Policies)

For technologies, project structure, and shell commands, read the plan:
[specs/011-media-watermark/plan.md](specs/011-media-watermark/plan.md)

Companion artifacts in the same folder:
- `spec.md` — user stories (US1 approved publisher uploads images on a draft, watermark applied, reorders, marks main, submits; US2 10-image / 2-video caps enforced client + server via BEFORE INSERT trigger AND UX disable; US3 rejected-resubmit preserves existing media in-place per Q3=A with row UUIDs intact; US4 watermark + downscale pipeline runs on a background isolate keeping UI ≥30 fps; US5 direct MP4 video upload — external-link UI deferred to a future spec per Q2=D; US6 storage-bucket RLS denies anon read on non-approved listings; US7 set-main / reorder / delete actions on the picker grid), FRs (FR-001..FR-023 — FR-023 is the AndroidManifest declaration for Q5=A permissions), 29 success criteria (SC-001..SC-029), clarifications (Session 2026-05-22 closed eight questions: Q1=A require ≥1 image at submit — submit_listing strengthened via FR-022; Q2=D disable external_link kind in Phase 11 — schema enum retained for forward-compat; Q3=A edit-in-place + Add more on the rejected-resubmit path — row UUIDs preserved per R-14; Q4=A bucket stores JPEG only but picker accepts broader source set (JPEG/PNG/HEIC/HEIF/WebP) and normalizes via FR-014 pipeline; Q5=A AndroidManifest declares both legacy READ_EXTERNAL_STORAGE AND granular READ_MEDIA_IMAGES/READ_MEDIA_VIDEO — image_picker plugin handles version-aware runtime requests; Q6=B 8000×8000 px hard cap via header-only metadata read BEFORE decode; Q7=B 60-second hard timeout per image enforced at BLoC layer; Q8=A public buckets + RLS on storage.objects as access filter — stable getPublicUrl() URLs for Phase 13 gallery caching)
- `research.md` — 20 locked plan-time decisions (R-21 four-migration apply order with R-35 immutable-Phase-10-amend-via-CREATE-OR-REPLACE; R-22 three new pubspec packages — image_picker + image + flutter_image_compress — the first deviation from Phase 10's R-03 zero-new-packages invariant, justified by Q4=A HEIC support; R-23 watermark exact params: bottom-end corner, 15% opacity, 18% of long edge, 24px padding, RTL-aware; R-24 header-only dimension reader covering JPEG SOF0 / PNG IHDR / HEIC ispe / WebP VP8X; R-25 shared sequential isolate worker model bounding peak RAM ~100 MB; R-26 bucket creation via SQL INSERT with ON CONFLICT (R-26) per Constitution II; R-27 storage.objects RLS policy SQL with split_part-based listing_id extraction + path-shape regex enforcement; R-28 client-side storage cleanup ordering (Storage DELETE → then SQL DELETE); R-29 cached_network_image consumption of getPublicUrl() URLs for Phase 13 gallery; R-30 cap-trigger error payload shape (SQLSTATE P0001 + DETAIL JSONB with current_count + max); R-31 Q1=A media check placement inside submit_listing as step 5a folded into v_missing accumulator; R-32 image_picker plugin handles version-aware permissions internally — no permission_handler package added; R-33 watermark asset path assets/images/watermark/logo_watermark.png + fail-closed on missing; R-34 quickstart device-coverage matrix Infinix Note 8 + Pixel 8 Pro emulator; R-35 Phase 10 migration immutability + amend-via-new-migration pattern; R-36 zero new Edge Functions in Phase 11; R-37 pubspec.lock regeneration with ~12 new transitive entries; R-38 per-thumbnail delete ordering (storage remove → SQL DELETE); R-39 60-second timeout enforced at BLoC layer; R-40 extend Phase 10's ListingFormBloc with five media events — no new BLoC. Plus carried-forward invariants R-01..R-20 from Phase 10 where applicable.
- `data-model.md` — the 1 new table (public.listing_media) with full CREATE TABLE body + CHECK constraints + partial unique index; the 2 new Supabase Storage buckets (listing-images public + JPEG only + 10 MB; listing-videos public + MP4 only + 30 MB); the 1 new BEFORE INSERT cap trigger body; the 1 new audit-trigger group on public.listing_media via Phase 4's log_audit() unchanged (R-05 invariant preserved a SEVENTH eighth time across Phases 4/5/6/7/8/9/10/11); the 7 RLS policies on public.listing_media; the 14 RLS policies on storage.objects (7 per bucket × 2 buckets); the FULL amended submit_listing RPC body inline; the Flutter entity (with two-value Dart enum image/video — external_link omitted from entity surface per Q2=D) + DTO + 6 use-case + 5 BLoC-event shapes; the watermark-pipeline pseudocode with R-23 params; the header-reader R-24 spec; the ARB-key inventory (~20 explicit keys); pubspec delta (3 new packages + flutter.assets entry); AndroidManifest delta (3 new <uses-permission> declarations); per-FR / per-SC verification map; cross-phase footprint summary
- `contracts/` — 10 interface contracts: `phase11-listing-media-table` (table column shape + CHECK constraints + partial unique index + trigger attachments), `phase11-storage-buckets` (bucket configs per Q4/Q8 + R-26 idempotent upsert), `phase11-storage-policies` (12 policies on storage.objects + R-27 path-shape regex + composite write-gate), `phase11-cap-trigger` (FR-004 trigger contract + R-30 error payload), `phase11-audit-triggers` (FR-005 log_audit() reuse + 3 new action keys + Phase 4 R-05 EIGHTH-time invariant), `phase11-rls-policies` (7 policies on public.listing_media + composite write-gate + Constitution III posture — no fourth anon carve-out), `submit-listing-amendment` (Phase 10 RPC + R-31 step 5a insertion + R-35 immutability + missing_fields[] extension), `media-picker-pages` (step_media + media_picker + media_thumbnail widget tree + R-40 BLoC event surface + Q-resolution enforcement table), `watermark-pipeline` (FR-014 7-step pipeline + R-23 watermark params + R-24 header reader + R-25 isolate + R-39 60s timeout), `video-file-validator` (FR-017 validator API + golden inputs table)
- `quickstart.md` — 15-step end-to-end manual verification recipe via Supabase MCP `apply_migration` / `execute_sql` / `get_advisors` + two-device Flutter UI walk (Infinix Note 8 for legacy READ_EXTERNAL_STORAGE path + Pixel 8 Pro emulator for granular READ_MEDIA_IMAGES/READ_MEDIA_VIDEO path); covers four-migration apply, schema verification including bucket flags + storage policy count, anonymous read deny against draft listing media, three-layer non-approved-publisher write deny, full happy-path upload+reorder+set-main+delete on Infinix Note 8 with SC-001 stopwatch, 10-image / 2-video cap fires at both layers, Q1=A media-minimum check at submit, Q3=A row UUIDs preserved across resubmit, Q4=A format normalization for HEIC/PNG/WebP + rejection of GIF/BMP/RAW, Q5=A version-aware permission walk on both devices, Q6=B pre-decode 8000×8000 reject, Q7=B 60-second timeout under throttled network, Q8=A status-flip immediately affects anon read, audit emission completeness matrix, and Constitution gate verification

Predecessors (still relevant — Phase 11 builds on top of them):
- [specs/010-listing-creation/plan.md](specs/010-listing-creation/plan.md) — Phase 10 listing-status engine + the 5 listing tables + submit_listing RPC. Phase 11 amends Phase 10's submit_listing via a new migration (Phase 10 file remains unedited per R-35) to add the Q1=A media-minimum check. Phase 11 extends Phase 10's ListingFormBloc with 5 media events per R-40 (no new BLoC). Phase 11 deletes Phase 10's step_media_placeholder.dart and mounts step_media.dart in its place. The Phase 10 submit_failure_dialog iterates `missing_fields[]` unchanged — Phase 11 only adds the new ARB key.
- [specs/009-currencies/plan.md](specs/009-currencies/plan.md) — Phase 9 currencies catalog. Phase 11 does NOT consume Phase 9 deliverables directly (the picker has no money / currency concerns).
- [specs/008-locations/plan.md](specs/008-locations/plan.md) — Phase 8 locations catalog + LocationPicker. Phase 11 does NOT consume Phase 8 deliverables directly (the picker has no location concerns).
- [specs/007-super-admin-roles/plan.md](specs/007-super-admin-roles/plan.md) — Phase 7 R-06 RPC-over-Edge-Function decision. Phase 11 carries forward R-36 (zero new Edge Functions); the submit_listing amendment uses the SECURITY DEFINER RPC pattern Phase 7 established.
- [specs/006-roles-permissions/plan.md](specs/006-roles-permissions/plan.md) — Phase 6 permission keys (`listings.view_all`, `listings.edit_any`) consumed unchanged by Phase 11 RLS policies + storage policies; no new permission keys (FR-009 / R-15).
- [specs/005-auth-profile/plan.md](specs/005-auth-profile/plan.md) — Phase 5 `profiles.publisher_status` + `account_status` consumed by Phase 11 RLS composite write-gate (FR-006) AND storage policies (FR-007); same composite as Phase 10 listings write-gate.
- [specs/004-supabase-foundation/plan.md](specs/004-supabase-foundation/plan.md) — Phase 4 `log_audit()` + `set_updated_at()` reusable trigger functions. Phase 11 attaches the EIGHTH audit-trigger group via `log_audit()` unchanged (R-05 invariant preserved across Phases 4/5/6/7/8/9/10/11), attaches `set_updated_at` to `public.listing_media` unchanged.
- [specs/003-localization/plan.md](specs/003-localization/plan.md) — Phase 3 ARB-driven localization. Phase 11 adds ~20 new ARB keys for picker chrome, validators, cap-exceeded messages, format/size/decode errors, the Q1=A images_below_minimum message, the Q5=A gallery-permission-denied message, the Q6=B image-too-large message, the Q7=B timeout message, the watermark-asset-missing fail-soft message.
- [specs/002-design-system/plan.md](specs/002-design-system/plan.md) — Phase 2 design tokens consumed by every new widget in lib/features/listing_form/presentation/widgets/ (step_media, media_picker, media_thumbnail).
- [specs/001-project-foundation/plan.md](specs/001-project-foundation/plan.md) — Phase 1 `cached_network_image` consumed by R-29 picker thumbnail re-mounts AND by Phase 13's forward-stated gallery (Phase 11 itself uses Image.memory for just-uploaded sessions per R-29).

Cross-cutting:
- [docs/decisions/0001-secrets-and-pii-storage.md](docs/decisions/0001-secrets-and-pii-storage.md) — Phase 11 introduces NO new Vault columns. Bucket objects are NOT considered PII (publisher self-publishes them); they are publicly readable when parent listing is approved per Q8=A.
- Forward-stated Phase 12 contract: Phase 12's `approve_listing` will flip the parent `listings.status` to `'approved'`, immediately changing the anon-readability of every bucket object under that listing via the storage RLS (no policy edit needed — the policy reads `listings.status` at request time per SC-025). Phase 12's admin preview surface will consume `listing_media` rows for moderation via `listings.view_all`.
- Forward-stated Phase 13 contract: Phase 13's public gallery widget will render `listing_media` rows ordered by `ordering ASC` with `is_main=true` first; consumes stable URLs from `getPublicUrl()` per Q8=A + R-29; caches via `cached_network_image`.
- Forward-stated Phase 14 contract: Phase 14's search-result thumbnails will read the main image of each approved listing — same stable-URL pattern as Phase 13.
- Forward-stated Phase 23 contract: a reconciliation job to clean up orphaned bucket objects (objects whose `listing_id` segment has no `listing_media` row OR whose row is gone) — deferred from Phase 11 per R-28.
- Forward-stated `external_link` UX deferral (per Q2=D): a future spec will introduce the "Add external link" CTA + URL validator + host allowlist. The schema enum value is reserved; the FR-005 2-video cap predicate already combines `kind IN ('video','external_link')` as defense-in-depth.
- Phase 22 follow-up (project memory `project_phase22_perm_cache_revisit.md`): Phase 22's push + Realtime will revisit Phase 6's three-point cache refresh; Phase 11 adds no new cache surface but the publisher's `media` field on `ListingFormState` is observable to the same lifecycle.
<!-- SPECKIT END -->
