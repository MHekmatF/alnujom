<!-- SPECKIT START -->
Active Spec Kit feature: `010-listing-creation` (Phase 10 — Listing Creation & Submit-for-Review)

For technologies, project structure, and shell commands, read the plan:
[specs/010-listing-creation/plan.md](specs/010-listing-creation/plan.md)

Companion artifacts in the same folder:
- `spec.md` — user stories (US1 approved publisher creates a draft through the seven-step form and submits to pending_review; US2 non-approved users blocked at all three layers — UX tile hidden, router guard refuses, RLS denies direct writes; US3 rejected listing is editable and resubmittable with full status-chain preservation; US4 publisher views their listings on MyListingsPage with status filter chips and rejection-reason rendering; US5 publisher enters a single primary price via the prices step — multi-currency entry deferred per Q3; US6 status-transition trigger auto-writes append-only listing_status_history on every status change; US7 validators for area, price, phone), FRs (FR-001..FR-021 plus FR-004a, FR-010a, FR-013a), 24 success criteria (SC-001..SC-024), clarifications (Session 2026-05-18 closed three questions: Q1=B Full required-field set with residential-property-type-conditional rooms+bathrooms; Q2=A area-centroid auto-fill with publisher unable to override in Phase 10 — Phase 15 adds pin-drop edit; Q3=A single-currency-only across every Phase 10 surface — no "Add another currency" affordance — Phase 9's UNIQUE(listing_id, currency_code) + partial-unique-on-is_primary schema support intact for future-spec extension)
- `research.md` — 20 locked technical decisions (R-01 date-prefixed migration filenames; R-02 inline policy bundling + parallel policy files; R-03 zero new pubspec packages; R-04 intentional deviation from Phase 8/9 anonymous-SELECT carve-out — listings are not global reference data, public-read gated by status='approved' + publish-window-open; R-05 log_audit reusable a seventh time across Phases 4/5/6/7/8/9/10; R-06 submit_listing is a SECURITY DEFINER RPC NOT an Edge Function — Phase 7/9 carry-forward deviation; R-07 area-centroid data-source path: ALTER public.areas + seed centroids from manually-researched OpenStreetMap data; R-08 trigger-before-seed audit ordering defensively preserved; R-09 status-transition trigger and log_audit are separate triggers — operational vs compliance separation; R-10 NUMERIC(14,2) for listing_prices.amount; R-11 listing_visibility parent-column-authoritative sync trigger; R-12 partial unique index (listing_id) WHERE is_primary=true on listing_prices; R-13 per-step auto-save granularity; R-14 draft mutated in place across resubmits — stable UUID preserves audit lineage; R-15 zero new permission keys; R-16 public-read-when-approved RLS shipped in Phase 10 — Phase 12's approval flip is a status change not a policy change; R-17 listings.agency_id ships without FK constraint — Phase 19 adds it; R-18 validators in lib/core/validators/ not lib/features/; R-19 publisher-status three-layer enforcement helper at lib/core/security/permission_checker.dart; R-20 no client-side cache on listings reads)
- `data-model.md` — the 5 new tables (`listings`, `listing_details`, `listing_prices`, `listing_visibility`, `listing_status_history`) with full CREATE TABLE bodies + check constraints + indexes; the 1 altered Phase 8 table (`public.areas` gains centroid_lat/centroid_lng with Syrian-bounds CHECK + seed); the 1 status-transition trigger (`listing_status_transition_trigger`) writing to listing_status_history on every INSERT and every UPDATE OF status; the 1 sync trigger (`listing_visibility_sync_trigger`) keeping parent-child location_visibility in sync; the 1 audit-trigger group on public.listings emitting 10 action keys (listing.created/.updated/.deleted + 7 status-delta verbs) via Phase 4's log_audit() unchanged; the 1 SQL view `v_publisher_listings` joining listings + most-recent listing_status_history (LATERAL LIMIT 1) + is_primary listing_prices for MyListingsPage's data layer; the 5 RLS policy bundles (one per table) with public-when-approved SELECT + owner-when-approved-pair write + admin via listings.edit_any/delete_any + append-only INSERT-trigger-only on listing_status_history; the submit_listing RPC body with Q1 Full required-field validation and SQLSTATE error contract; the empty seed inventory (Phase 10 seeds zero listings; areas centroid seed lives in migration 1); the BLoC + entity + use-case shapes for the new lib/features/listing_form/ and lib/features/publisher_dashboard/ feature folders; the 3 validator API shapes; the ARB-key inventory (48 explicit keys across 8 categories: form chrome / field labels / amenities catalog / validator errors / status badges / rejection-resubmit / missing-field labels / submit errors / publisher-dashboard tiles + pending-approval screen); per-FR / per-SC verification map
- `contracts/` — 13 interface contracts: `phase10-tables` (5 new tables with column/constraint/RLS shapes), `phase10-altered-areas` (centroid columns + Syrian-bounds CHECK + manual OpenStreetMap seed inventory), `phase10-status-transition-trigger` (AFTER INSERT + AFTER UPDATE OF status; append-only history-row emission), `phase10-audit-triggers` (log_audit() reuse with the 10 action key matrix), `phase10-rls-policies` (per-table policy inventory + Constitution III posture: no broad anon carve-out, public-when-approved gate, append-only history via pg_trigger_depth()), `phase10-v-publisher-listings` (v_publisher_listings view: LEFT JOIN LATERAL for most-recent history + LEFT JOIN listing_prices(is_primary) + RLS inheritance + GRANT SELECT TO authenticated), `submit-listing-rpc` (SECURITY DEFINER PL/pgSQL signature + Q1 Full required-field check + SQLSTATE error contract + structured missing_fields[] DETAIL payload), `listing-form-pages` (seven-step form: step-by-step field inventory + BLoC event surface + Q1/Q2/Q3 enforcement), `my-listings-page` (status-filter chips + rejection-reason rendering + resubmit CTA + read-only preview path), `area-centroid-autofill` (FR-013a contract: lookup + step-block on missing data + no-publisher-override invariant), `validators` (3 validator APIs with manual golden inputs/outputs), `listings-routing` (3 new go_router routes + publisher-status redirect guards + publisher-dashboard tile gate), `listings-localization` (ARB keys + Syrian-friendly Arabic copy + status-badge label set + structured-error key shape)
- `quickstart.md` — 14-step end-to-end manual verification recipe via Supabase MCP `apply_migration` / `execute_sql` / `get_advisors` + Flutter UI walk on Infinix Note 8 against the remote Supabase project; covers seven-migration apply, schema verification, anonymous read deny, three-layer non-approved-publisher gate (UX/router/RLS), full happy-path create→submit on device, schema state inspection (5 child tables + 2 history rows + correct audit-log emission), Q1 validation deny (missing fields error contract), append-only history (UPDATE/DELETE refused), rejected-resubmit loop with full status-chain preservation, full status walk (audit emission matrix), anonymous public-read of approved listing, single-currency invariant (SC-022), and mid-session publisher_status approval propagation (SC-020)

Predecessors (still relevant — Phase 10 builds on top of them):
- [specs/009-currencies/plan.md](specs/009-currencies/plan.md) — Phase 9 currencies catalog + Money value object + MoneyFormatter + the SECURITY DEFINER RPC pattern carried forward from Phase 7. Phase 10 consumes Phase 9's `MoneyFormatter` unchanged for the prices step's inline preview and for `MyListingsPage`'s primary-price rendering. The Phase 9 forward-stated `listing_prices` schema shape (UNIQUE(listing_id, currency_code) per Q4; currency_code FK to public.currencies(code) ON DELETE RESTRICT per FR-006; exactly-one-is_primary-true per Phase 9 Assumption 10) is honored verbatim in Phase 10's migration 4. The FR-019a row-selection rule from Phase 9 is consumed unchanged by Phase 13+ (Phase 10 has only one row per listing per Q3, so the rule's primary-fallback branch always succeeds).
- [specs/008-locations/plan.md](specs/008-locations/plan.md) — Phase 8 locations catalog + the `LocationPicker` widget. Phase 10 reuses LocationPicker verbatim in the form's location step (SC-021). Phase 10 ALTERs Phase 8's `public.areas` table to add `centroid_lat`/`centroid_lng` columns per R-07 + Q2 — this is the first Phase 8 schema mutation by any later phase. Phase 8's DEFERRED.md forecasted that Phase 10 would carry coordinates; the centroid columns satisfy that forecast at the area-grain.
- [specs/007-super-admin-roles/plan.md](specs/007-super-admin-roles/plan.md) — Phase 7 RPC pattern (`mutate_role`). Phase 10 carries forward Phase 7's R-06 + Phase 9's R-06 decision: prefer a PL/pgSQL RPC over a TypeScript Edge Function for atomic permission-checked mutations. The implementation plan's literal "Edge Function `submit_listing`" text is deviated from per Phase 10 research R-06.
- [specs/006-roles-permissions/plan.md](specs/006-roles-permissions/plan.md) — Phase 6 introduced the `listings.view_all`/`approve`/`reject`/`edit_any`/`delete_any` permission keys in `public.permissions` and the role mappings. Phase 10 reuses these keys without adding any new permission (FR-008). Phase 6's `current_user_has_permission(TEXT)` helper is consumed unchanged by every Phase 10 admin write policy. Phase 6's PermissionChecker cache-refresh invariant is extended (R-19) to the new `userIsApprovedPublisher()` helper for the publisher-status gate.
- [specs/005-auth-profile/plan.md](specs/005-auth-profile/plan.md) — Phase 5 auth + profile/publisher-dashboard + the `profiles.publisher_status` column (`pending` / `approved` / `rejected` / `suspended` / `deleted`). Phase 10 reads `publisher_status='approved' AND account_status='approved'` to gate the "Create listing" tile, the router guard, and the RLS write policies. Phase 5's publisher dashboard surface is extended with two new entry tiles ("Create listing", "My listings"); Phase 5's auth-redirect helper is extended with publisher-status redirect rules.
- [specs/004-supabase-foundation/plan.md](specs/004-supabase-foundation/plan.md) — Phase 4 `log_audit()` + `set_updated_at()` reusable trigger functions + the listing-status enum. Phase 10 attaches new audit-trigger group on `public.listings` via the unchanged `log_audit` function (R-05 reusability invariant preserved a **seventh** time across Phases 4/5/6/7/8/9/10), attaches `set_updated_at` to four of the five new tables unchanged.
- [specs/003-localization/plan.md](specs/003-localization/plan.md) — Phase 3 ARB-driven localization. Phase 10 adds ~40 new ARB keys for the form chrome, validators, status badges, rejection-reason rendering, and submit_listing structured errors. Bilingual data labels (governorate/city/area from Phase 8; currency name+symbol from Phase 9) come from the respective tables' bilingual columns, not from ARB.
- [specs/002-design-system/plan.md](specs/002-design-system/plan.md) — Phase 2 design tokens consumed by every new widget in `lib/features/listing_form/presentation/` and `lib/features/publisher_dashboard/presentation/`.

Cross-cutting:
- [docs/decisions/0001-secrets-and-pii-storage.md](docs/decisions/0001-secrets-and-pii-storage.md) — Supabase Vault for secrets and admin-only PII. Phase 10 stores `listings.phone` and `listings.whatsapp` as plain columns (publisher self-disclosed contact channels published with their listing); admin-only Vault columns ship in Phase 16 (`inquiries.inquirer_phone`) and Phase 19 (`agency_verification_requests.id_document_number`). Phase 10 introduces no new Vault columns.
- Forward-stated Phase 11 contract: Phase 11 introduces `public.listing_media` (1:N from listings) + storage policies for `listing-images`/`listing-videos` buckets + client-side watermark + 10-image / 2-video caps. Phase 10's form has a no-op media-placeholder step at position 6 which Phase 11 replaces with the actual MediaPicker widget. Phase 10's `listings` table has no media-related columns; all media lives in the Phase 11 child table.
- Forward-stated Phase 12 contract: Phase 12 introduces `approve_listing` / `reject_listing` RPCs that flip `listings.status` from `pending_review` to `approved`/`rejected` AND set the rejection reason on the most-recent `listing_status_history` row. The Phase 10 status-transition trigger writes the history row with `reason=NULL`; Phase 12's RPC writes the reason via either an UPDATE-from-trigger-context (allowed by adjusting the RLS policy) or a separate INSERT-side-effect — exact mechanism is Phase 12's design choice. The public-read-when-approved RLS is already shipped in Phase 10; Phase 12 only needs the status flip.
- Forward-stated Phase 13/14/15 contracts: Phase 13 (Public listing details), Phase 14 (Search & filters), and Phase 15 (Map view) consume Phase 10's tables + the FR-019a row-selection rule (which for Phase 10 listings always returns the single primary row). Phase 15 specifically reads `public.areas.centroid_lat`/`centroid_lng` to render default pin placement when `listing_visibility='approximate'` (per R-07's Phase 15 alignment) AND reads `listings.latitude`/`longitude` (centroid-derived in Phase 10; pin-drop-editable in Phase 15 onward) for `location_visibility='exact'`.
- Forward-stated Phase 19 contract: Phase 19 introduces `public.agencies` and adds the FK constraint on `listings.agency_id REFERENCES public.agencies(id) ON DELETE SET NULL`. Phase 10 ships the column unconstrained (R-17); Phase 19's migration enforces the FK once the agencies table exists.
- Phase 22 follow-up (project memory `project_phase22_perm_cache_revisit.md`): Phase 22's push + Realtime will revisit Phase 6's three-point cache refresh; Phase 10's mutation surface (publisher_status grants via Phase 5 admin flow propagating to the "Create listing" tile) adds another data point worth examining at that time.
<!-- SPECKIT END -->
