<!-- SPECKIT START -->
Active Spec Kit feature: `006-roles-permissions` (Phase 6 — Roles & Permissions)

For technologies, project structure, and shell commands, read the plan:
[specs/006-roles-permissions/plan.md](specs/006-roles-permissions/plan.md)

Companion artifacts in the same folder:
- `spec.md` — user stories (US1 seeded catalog, US2 is_admin replacement, US3 profile-page roles, US4 permission-gated admin tile, US5 cache refresh), FRs, success criteria, clarifications (Session 2026-05-15 closed three questions: first super_admin bootstrap = post-Phase-6 privileged SQL (Option C, mirrors Phase 5 R-19); PermissionChecker cache refresh = three observation points exactly (auth state listener, app foreground resume, AuthBloc.refreshSession() — Phase 22 spec is tagged to revisit Realtime); PII cross-user read predicate = keep `current_user_is_admin()` gate (admins decrypt, moderators do not))
- `research.md` — 22 locked technical decisions (R-01 migration filename timestamps + divergence from IMPLEMENTATION_PLAN.md naming; R-02 inherited apply mechanism; R-03 inline-seed-in-migration; R-04 admin = 16 rows including agencies.view + inquiries.view_all; R-05 central-helper invariant preserved a second time; R-06 system-role immutability via BEFORE UPDATE/DELETE trigger; R-07 hand-maintained Dart constants; R-08 PermissionChecker = `Set<String>` cache; R-09 lazy singleton via get_it; R-10 single-round-trip nested Postgrest fetch; R-11 backfill ordering; R-12 current_user_is_admin body swap mechanism; R-13 cross-user profiles read via stacked policy file; R-14 auto-user-role trigger placement; R-15 user_roles audit trigger via log_audit reuse; R-16 first super_admin bootstrap (Q1); R-17 cache refresh strategy (Q2); R-18 PII cross-user predicate retention (Q3); R-19 PermissionKeys class layout; R-20 audit action-key convention; R-21 lint guard optional; R-22 Profile entity isAdmin field removal)
- `data-model.md` — the 4 new tables (`roles`, `permissions`, `role_permissions`, `user_roles`) with full schemas + RLS posture; the 2 helper functions (`current_user_has_permission(perm_key)`, body-swapped `current_user_is_admin()`); the 3 new triggers (system-role immutability on roles, auto-user-role on profiles INSERT, audit on user_roles); the 1 new cross-user `profiles` read policy stacked on top of Phase 4's self-only; the seed contents (7 system roles, 24 permission keys, 45 role-permission mappings); the Flutter `PermissionChecker` state shape; the `PermissionKeys` Dart constants layout
- `contracts/` — 13 interface contracts: `roles-table`, `permissions-table`, `role-permissions-table`, `user-roles-table`, `permission-predicate` (new `current_user_has_permission` helper), `admin-predicate-v6` (body swap of Phase 5's `current_user_is_admin`), `permission-checker` (Flutter singleton), `permission-keys-dart` (the constants layout), `system-role-immutability-trigger`, `auto-user-role-trigger`, `user-roles-audit-trigger` (reuses Phase 4 log_audit unchanged), `profiles-users-view-policy` (the stacked cross-user read), `is-admin-backfill-migration` (the FR-011 transactional backfill + column drop)
- `quickstart.md` — 20-step end-to-end manual verification recipe via Supabase MCP `execute_sql` / `apply_migration` / `get_advisors` + Flutter UI walk on Infinix Note 8 against the remote Supabase project; no automated tests; Step 18 documents the post-Phase-6 first-super_admin bootstrap SQL

Predecessors (still relevant — Phase 6 builds on top of them):
- [specs/005-auth-profile/plan.md](specs/005-auth-profile/plan.md) — Phase 5 auth/profile/admin-queue/Vault-PII. Phase 6 swaps Phase 5's `current_user_is_admin()` body (no policy files edited); drops the interim `profiles.is_admin` column; converts every prior admin to a `user_roles` row; rehosts the Phase 5 admin queue under a new admin home tile.
- [specs/004-supabase-foundation/plan.md](specs/004-supabase-foundation/plan.md) — Phase 4 source-controlled backend skeleton; `log_audit()` reusable trigger Phase 6 attaches to `user_roles` unchanged; Phase 4 RLS-default-block covers anon access to Phase 6 tables.
- [specs/003-localization/plan.md](specs/003-localization/plan.md) — Phase 3 ARB-driven localization. Phase 6's `roles.display_name` JSONB column is keyed by locale (`ar`, `en`) matching Phase 3's convention; missing translations fall back to the role's `key`.
- [specs/002-design-system/plan.md](specs/002-design-system/plan.md) — design tokens consumed by the new admin home page and the profile-page Roles section.

Cross-cutting:
- [docs/decisions/0001-secrets-and-pii-storage.md](docs/decisions/0001-secrets-and-pii-storage.md) — Supabase Vault for secrets and admin-only PII. Phase 6 does NOT change Phase 5's Vault PII helpers (Q3 — Option A keeps the `current_user_is_admin()` gate).
- Phase 22 follow-up (tagged in project memory `project_phase22_perm_cache_revisit.md`): when Phase 22 ships push + Realtime, the spec MUST revisit Phase 6's three-point cache refresh and consider adding a Realtime subscription on `user_roles`.
<!-- SPECKIT END -->
