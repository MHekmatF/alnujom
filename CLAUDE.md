<!-- SPECKIT START -->
Active Spec Kit feature: `009-currencies` (Phase 9 — Currencies & Exchange Rates)

For technologies, project structure, and shell commands, read the plan:
[specs/009-currencies/plan.md](specs/009-currencies/plan.md)

Companion artifacts in the same folder:
- `spec.md` — user stories (US1 public-read currencies seed; US2 admin entry tile + three-layer gate; US3 admin sets a new rate via `update_exchange_rate` with auto-derived inverse; US4 user preferred-currency toggle picks among multi-currency listing rows — NOT a conversion trigger; US5 listing prices render in publisher's entered currency without conversion; US6 admin views exchange-rate history with derived badge; US7 MoneyFormatter renders RTL Arabic digits / locale-appropriate symbols / no rate parameter; US8 audit triggers on both tables), FRs (FR-001..FR-026), 25 success criteria, clarifications (Session 2026-05-17 closed five questions: Q1 NO display-time currency conversion ever — listings render in publisher-entered currency; Q2 server-side auto-derive of inverse rate with `source='auto-derived from <uuid>'`; Q3 vacated by Q1; Q4 forward-stated `UNIQUE(listing_id, currency_code)` on Phase 10's `listing_prices`; Q5 symmetric 24-hour confirmation gate for backdated rates mirroring the existing future-dating gate)
- `research.md` — 22 locked technical decisions (R-01 date-prefixed migration filenames; R-02 inline policy bundling + parallel policy files; R-03 one new pubspec package: `decimal: ^3.0.0` for Money.amount; R-04 anonymous SELECT carve-out on both tables — third project-wide; R-05 log_audit reusable a sixth time; R-06 `update_exchange_rate` is a SECURITY DEFINER RPC NOT an Edge Function — Phase 7 carry-forward deviation from implementation plan literal text; R-07 is_system + immutability trigger on currencies mirroring Phase 6/8; R-08 trigger-before-seed audit ordering; R-09 decimal package for high-precision amounts; R-10 NUMERIC(18,6) for exchange_rates.rate; R-11 banker's rounding half-to-even for auto-derived inverse; R-12 custom ل.س symbol override in ar locale; R-13 FR-019a row-selection rule lives in select_listing_price_row.dart; R-14 user_preferences FK addition is a separate migration after seed; R-15 no new permission keys; R-16 anonymous SELECT carve-out documented in migration comments; R-17 MoneyFormatter is pure-function with no global state; R-18 locale fallback chain on currency-name labels; R-19 compound dependency-aware delete dialog; R-20 no client-side cache on currencies catalog or rates; R-21 MoneyFormatterShowcasePage debug-only smoke-test surface; R-22 missing-display-currency-match fallback shows native price with discreet hint)
- `data-model.md` — the 2 new tables (`currencies`, `exchange_rates`) with full CREATE TABLE bodies + check constraints + composite index; the 4 audit triggers (3 on currencies + 1 on exchange_rates) via Phase 4's `log_audit()` unchanged; the 1 immutability trigger (`enforce_currency_system_immutability`) with body source; the 6 RLS policies (4 on currencies + 2 on exchange_rates including explicit deny on UPDATE/DELETE for append-only invariant); the `update_exchange_rate` RPC body with atomic two-INSERT and Q2 auto-derive; the seed inventory (USD + SYP + optional starter rate pair); the BLoC + entity + use-case shapes for the new `lib/features/currencies/` feature folder; the Money value object + MoneyFormatter API shapes for `lib/shared/`; the ARB-key inventory (~25 page/dialog/error/form/badge keys); per-FR / per-SC verification map at the end
- `contracts/` — 13 interface contracts: `phase9-tables` (2 tables with column/constraint/RLS shapes), `phase9-audit-triggers` (4 triggers reusing log_audit + trigger-before-seed ordering), `phase9-immutability-trigger` (1 BEFORE-UPDATE-OR-DELETE trigger refusing DELETE / code-rename on is_system=true currencies), `phase9-rls-policies` (4 policies on currencies + 4 on exchange_rates with explicit deny on UPDATE/DELETE), `update-exchange-rate-rpc` (SECURITY DEFINER PL/pgSQL function signature + atomic two-INSERT + Q2 auto-derive + JSONB return shape + SQLSTATE error contract), `currencies-seed` (USD+SYP+optional starter rate pair inventory + idempotency contract), `money-value-object` (Money domain entity: Decimal amount + currencyCode, no rate field, Constitution IX-clean imports), `money-formatter` (MoneyFormatter pure-function API: no rate parameter, locale-driven digit form + symbol position, R-12 ل.س override), `currencies-admin-pages` (4 pages: CurrenciesListPage, CurrencyFormPage, SetExchangeRatePage, ExchangeRateHistoryPage + system-row affordance hiding + derived-badge rendering + unusual-timing dialog), `preferred-currency-toggle` (profile-page toggle: read user_preferences.display_currency, write via authenticated UPDATE, deactivated/NULL fallback), `listing-price-row-selection` (FR-019a deterministic selection rule for Phase 13/14/15), `currencies-routing` (admin-home tile + 4 go_router routes + redirect guards), `currencies-localization` (ARB keys + Syrian-friendly Arabic copy + bilingual name_ar/name_en fallback chain)
- `quickstart.md` — 12-step end-to-end manual verification recipe via Supabase MCP `apply_migration` / `execute_sql` / `get_advisors` + Flutter UI walk on Infinix Note 8 against the remote Supabase project; includes the 10 MoneyFormatter golden cases (banker's rounding, RTL Arabic, zero, negative, decimals-rounding) as a manual checklist per R-21; per-FR / per-SC verification map at the end of the file; Step 12 covers the cross-device propagation test for SC-021 (mid-session permission revoke via Phase 7 surface) and the deactivated-currency fallback (US4 acceptance scenario 4)

Predecessors (still relevant — Phase 9 builds on top of them):
- [specs/008-locations/plan.md](specs/008-locations/plan.md) — Phase 8 locations catalog + the per-table is_system / immutability-trigger pattern + the per-table audit-trigger group pattern + the anon-SELECT carve-out precedent + the bilingual data-label rendering pattern (Phase 8 used JSONB display_name; Phase 9 adapts to parallel TEXT name_ar/name_en columns per implementation plan §6.2). Phase 9 reuses the trigger-attach pattern, the per-table policy-file pattern, the route-guard helper, and the admin-home tile pattern. Phase 9 does not edit any Phase 8 artifact.
- [specs/007-super-admin-roles/plan.md](specs/007-super-admin-roles/plan.md) — Phase 7 super-admin role UI + the SECURITY DEFINER RPC pattern (`mutate_role`). Phase 9 carries forward Phase 7's R-06 decision: prefer a PL/pgSQL RPC over a TypeScript Edge Function for atomic permission-checked mutations. The implementation plan's literal "Edge Function `update_exchange_rate`" text is deviated from per Phase 9 research R-06.
- [specs/006-roles-permissions/plan.md](specs/006-roles-permissions/plan.md) — Phase 6 introduced the `currencies.manage` permission key in `public.permissions` and mapped it to `admin` (§9.1) and `super_admin` (full catalog). Phase 9 reuses the key without adding any new permission. Phase 6's `current_user_has_permission(TEXT)` helper is consumed unchanged by every Phase 9 write policy and inside the `update_exchange_rate` RPC. Phase 6's `enforce_role_system_immutability` trigger is the precedent for Phase 9's `enforce_currency_system_immutability` trigger.
- [specs/005-auth-profile/plan.md](specs/005-auth-profile/plan.md) — Phase 5 auth + admin home + profile/settings page. Phase 9 adds one tile to the Phase 6-rehosted `AdminHomePage` and adds one toggle widget to the existing Phase 5 profile/settings page (`PreferredCurrencyToggle`). Other Phase 5 surfaces are not edited.
- [specs/004-supabase-foundation/plan.md](specs/004-supabase-foundation/plan.md) — Phase 4 `log_audit()` + `set_updated_at()` reusable trigger functions + the `user_preferences.display_currency` column (existing TEXT default 'SYP'). Phase 9 attaches two new audit-trigger groups via the unchanged `log_audit` function (R-05 reusability invariant preserved a **sixth** time across Phases 4/5/6/7/8/9), attaches `set_updated_at` to the new `currencies` table unchanged, and adds a FK constraint on `user_preferences.display_currency` referencing `public.currencies(code) ON DELETE SET NULL`.
- [specs/003-localization/plan.md](specs/003-localization/plan.md) — Phase 3 ARB-driven localization + the `intl` package for number/currency formatting. Phase 9 adds ~25 new ARB keys for the currencies admin pages, the profile-page toggle, and the derived-badge label; the MoneyFormatter consumes `intl` for digit/separator/symbol-position rendering with a custom ل.س symbol override for SYP in ar locale (R-12).
- [specs/002-design-system/plan.md](specs/002-design-system/plan.md) — Phase 2 design tokens consumed by every new widget in `lib/features/currencies/presentation/`.

Cross-cutting:
- [docs/decisions/0001-secrets-and-pii-storage.md](docs/decisions/0001-secrets-and-pii-storage.md) — Supabase Vault for secrets and admin-only PII. Phase 9 does NOT introduce any PII or secret storage; the currencies catalog and exchange-rate history are global reference data with no privacy concern.
- Forward-stated Phase 10 contract: when Phase 10 introduces `listing_prices`, the table MUST carry (a) a `UNIQUE(listing_id, currency_code)` constraint per Q4, (b) a `currency_code TEXT NOT NULL REFERENCES public.currencies(code) ON DELETE RESTRICT` FK per FR-006 / Assumption 10, (c) support for multiple rows per listing with exactly one `is_primary=true` per the implementation plan §8. The Phase 9 row-selection rule (FR-019a / `select_listing_price_row.dart`) relies on all three contracts being honored.
- Forward-stated Phase 13/14/15 contracts: Phase 13 (Public listing details), Phase 14 (Search & filters), and Phase 15 (Map view) will consume the FR-019a row-selection rule + `MoneyFormatter` to render listing prices in the publisher's entered currency (per Q1 no-conversion stance). The anon-SELECT carve-out on `currencies` enables this for anonymous viewers. Phase 14's price-range filter is constrained to filter on native amounts only per the Q1 / Q3 resolution.
- Phase 22 follow-up (project memory `project_phase22_perm_cache_revisit.md`): Phase 22's push + Realtime will revisit Phase 6's three-point cache refresh; Phase 9's mutation surface (admin grants / revokes the `currencies.manage` permission via Phase 7's RoleEditorPage; admin creates / renames / deactivates currencies; admin sets new rates via `update_exchange_rate` RPC) adds another data point worth examining at that time, though Phase 9 itself adds no new propagation work — the currencies catalog reads fresh on each mount (R-20).
<!-- SPECKIT END -->
