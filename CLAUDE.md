<!-- SPECKIT START -->
Active Spec Kit feature: `007-super-admin-roles` (Phase 7 — Super-admin role UI)

For technologies, project structure, and shell commands, read the plan:
[specs/007-super-admin-roles/plan.md](specs/007-super-admin-roles/plan.md)

Companion artifacts in the same folder:
- `spec.md` — user stories (US1 super-admin entry tile + route guard, US2 RolesListPage system-row protection, US3 RoleEditorPage atomic save, US4 custom-role lifecycle, US5 user-role assign/revoke with mid-session propagation, US6 audit-trail expansion), FRs (FR-001..FR-021), 27 success criteria, clarifications (Session 2026-05-15 closed six questions: Q1 super_admin in-app grants allowed via two-step typed-match confirmation; Q2 self-revocation of super_admin blocked unconditionally; Q3 mutation entry point is SECURITY DEFINER SQL RPC `public.mutate_role`, not an Edge Function — the IMPLEMENTATION_PLAN.md Edge Function path is deferred per Constitution XII; Q4 concurrent role edits mediated by optimistic locking via the existing `roles.updated_at` token; Q5 the `super_admin` role's permission mapping is immutable through `RoleEditorPage` and through `mutate_role` server-side; Q6 permission-category headings localized via 12 new `permissionCategory<Capitalized>` ARB keys, no DB schema change)
- `research.md` — 22 locked technical decisions (R-01 migration filename convention carry forward; R-02 inline policy bundling preserved; R-03 first super_admin bootstrap = post-Phase-6 privileged SQL, carry forward of Phase 6 Q1 Option C; R-04 two-step super_admin grant; R-05 unconditional super_admin self-revoke block; R-06 SECURITY DEFINER SQL RPC not Edge Function; R-07 optimistic locking via roles.updated_at; R-08 super_admin permission-set immutability; R-09 ARB-keyed category localization; R-10 5-migration consolidation; R-11 mutate_role positional args; R-12 audit-row-per-mutation-row granularity; R-13 phone+username search; R-14 SQLSTATE→ARB error catalog; R-15 admin-home-tile entry point; R-16 server-side permission delta; R-17 Phase 6 three-point cache refresh preserved; R-18 super_admin self-grant blocked by UNIQUE; R-19 reusable confirmation widget; R-20 no bulk ops in v1; R-21 agency_admin 0-row mapping preserved; R-22 DEFERRED.md treatment)
- `data-model.md` — the 3 new SECURITY DEFINER RPCs (`mutate_role`, `assign_role_to_user`, `revoke_role_from_user`) with full signatures + bodies + error contracts; the 3 new audit-trigger groups (on `roles`, `role_permissions`, `permissions`); the 3 new write-side RLS policies (on `roles`, `role_permissions`, `user_roles`); the `permission_keys.dart` delta (`superAdminCategoryKeys`); the BLoC + entity + use-case shapes for the new `lib/features/super_admin/` feature folder; the ARB-key inventory (12 permission-category keys + ~25 page/dialog/error keys)
- `contracts/` — 9 interface contracts: `phase7-audit-triggers` (3 trigger groups via Phase 4 log_audit unchanged), `phase7-write-policies` (write-side RLS on the four Phase 6 catalog tables), `mutate-role-rpc` (atomic role + permissions delta with optimistic-lock + super_admin permission-set immutability), `assign-role-to-user-rpc` (with two-step super_admin confirmation), `revoke-role-from-user-rpc` (with unconditional self-revoke block), `super-admin-routing` (admin-home tile + 4 routes + route guards), `role-editor-page` (page contract + RoleEditorBloc with expectedUpdatedAt token capture + super_admin checklist read-only), `assign-role-page` (search + drawer + two-step super_admin grant + self-row revoke suppression), `permission-category-localization` (ARB-key pattern + 12-key inventory)
- `quickstart.md` — 12-step end-to-end manual verification recipe via Supabase MCP `apply_migration` / `execute_sql` / `get_advisors` + Flutter UI walk on Infinix Note 8 (single device + optional second device for the SC-011 mid-session propagation test) against the remote Supabase project; per-FR / per-SC verification map at the end of the file; Step 2 documents the post-Phase-6 first-super_admin bootstrap SQL (Phase 7 deploy prerequisite)

Predecessors (still relevant — Phase 7 builds on top of them):
- [specs/006-roles-permissions/plan.md](specs/006-roles-permissions/plan.md) — Phase 6 catalog tables (`roles`, `permissions`, `role_permissions`, `user_roles`), the 24 seeded permission keys, the seven seeded system roles, the §9.1 default role-permission mappings, the `current_user_has_permission` and (body-swapped) `current_user_is_admin` helpers, the system-role-immutability trigger, the auto-user-role trigger, the user_roles audit triggers, the `lib/core/security/PermissionChecker` with three observation points, the `AdminHomePage` tile-listing skeleton. Phase 7 adds the in-app mutation surface on top.
- [specs/005-auth-profile/plan.md](specs/005-auth-profile/plan.md) — Phase 5 auth + admin queue (rehosted by Phase 6 under `AdminHomePage`). Phase 7 does not edit Phase 5 surfaces; the admin queue continues to function unchanged.
- [specs/004-supabase-foundation/plan.md](specs/004-supabase-foundation/plan.md) — Phase 4 `log_audit()` reusable trigger function. Phase 7 attaches three new trigger groups via the unchanged function (Phase 4 R-05 reusability invariant preserved a third time).
- [specs/003-localization/plan.md](specs/003-localization/plan.md) — Phase 3 ARB-driven localization. Phase 7 adds 12 `permissionCategory<Capitalized>` keys plus ~25 page/dialog/error keys.
- [specs/002-design-system/plan.md](specs/002-design-system/plan.md) — Phase 2 design tokens consumed by every new widget in `lib/features/super_admin/presentation/`.

Cross-cutting:
- [docs/decisions/0001-secrets-and-pii-storage.md](docs/decisions/0001-secrets-and-pii-storage.md) — Supabase Vault for secrets and admin-only PII. Phase 7 does NOT change Phase 5's Vault PII helpers; the Phase 6 Q3 gate (admins decrypt; moderators do not) is preserved.
- Phase 22 follow-up (tagged in project memory `project_phase22_perm_cache_revisit.md`): when Phase 22 ships push + Realtime, the spec MUST revisit Phase 6's three-point cache refresh in light of Phase 7's additional mutation surface (every grant/revoke from `AssignRolePage` and every permission edit from `RoleEditorPage` produces propagation work).
- Deferral note: the IMPLEMENTATION_PLAN.md §Phase 7 Edge Function `mutate_role` path is **deferred** per Clarifications Q3. The Phase 7 RPC achieves the same atomicity guarantee with zero new deployment infrastructure (Supabase MCP `apply_migration` only). When a later spec genuinely needs Edge Function infrastructure (e.g., Phase 22 FCM fan-out reading Vault secrets), that spec MAY revisit whether to migrate `mutate_role` for unified observability.
<!-- SPECKIT END -->
